# EDK — 시스템 흐름 (System Flow)

> 원본: pmg-ic-pbl/docs/system_flow.md
> 상태: EDK 전환 중 — 화면 흐름 업데이트 필요

---

## 1. 전체 시스템 상태 머신 (EDK 기준)

```
                    ┌─────────┐
               ┌───►│  IDLE   │◄──────────────────┐
               │    └────┬────┘                   │
               │         │ 터치                    │ 결제완료/취소
               │    ┌────▼──────────┐             │
               │    │ SYMPTOM_SELECT│             │
               │    └──┬────────────┘             │
               │       │ 증상 선택                 │
               │  응급  │                          │
               │  ┌────▼──────┐                   │
               │  │ EMERGENCY │                   │
               │  └───────────┘                   │
               │       │ 일반 증상                 │
               │    ┌──▼──────────┐               │
               │    │MEDICINE_LIST│               │
               │    └──┬──────────┘               │
               │       │ 제품 선택                 │
               │    ┌──▼────────────┐             │
               │    │MEDICINE_DETAIL│             │
               │    └──┬────────────┘             │
               │       │ 장바구니 담기             │
               │    ┌──▼──────────┐               │
               │    │CART_VIEWING │               │
               │    └──┬──────────┘               │
               │       │ 결제 진행                 │
               │    ┌──▼──────────────┐           │
               │    │ PAYMENT_SELECT  │           │
               │    └──┬──────────┬───┘           │
               │  현금 │          │ 카드           │
               │  ┌────▼───┐ ┌────▼──────────┐   │
               │  │CASH_   │ │CARD_PAYMENT   │   │
               │  │PAYMENT │ └───────────────┘   │
               │  └────┬───┘        │            │
               │       └────────────┘            │
               │            │ 완료               │
               │    ┌────────▼───────┐           │
               │    │PAYMENT_COMPLETE├───────────┘
               │    └────────────────┘
               │
               │ 관리자 선택
          ┌────┴──────────┐
          │  ADMIN_AUTH   │
          └──────┬────────┘
                 │
          ┌──────▼────────┐
          │  ADMIN_MODE   │
          └───────────────┘
```

---

## 2. 증상 선택 흐름

```
[증상 선택 화면]
  │
  증상 카테고리 표시:
  1. 두통 / 열
  2. 감기 (코막힘/인후통)
  3. 소화불량 / 속쓰림
  4. 피로 / 영양 보충
  5. 외상 / 상처
  6. 관리자 모드
  │
  ├─ 응급 증상 감지 → [응급 안내 화면] → IDLE
  └─ 일반 증상 선택 → [제품 목록 화면]
```

---

## 3. 제품 탐색 흐름

```
[제품 목록 화면]
  │
  선택한 증상에 해당하는 제품 목록 표시
  (품절 제품 → 선택 불가 처리)
  │
  제품 선택 → [제품 상세 화면]
    효능 / 복용법 / 주의사항 / 가격 / 재고 표시
    [장바구니 담기] → [장바구니 화면]
    [계속 쇼핑] → [증상 선택 화면]
```

---

## 4. 결제 흐름 (기존 로직 재활용)

```
[장바구니] → [결제 수단 선택]
  ├── 현금: 권종 누적 투입 → 잔돈 계산 (그리디) → 영수증
  └── 카드: 즉시 성공 시뮬레이션 → 영수증
```

---

## 5. 예외 흐름

| 예외 | 발생 위치 | 처리 |
|------|---------|------|
| `InsufficientChangeException` | 현금 잔돈 계산 | 투입금 반환, 장바구니 복귀 |
| `PaymentException` | 카드 결제 | 결제 수단 선택으로 복귀 |
| `InsufficientStockException` | 재고 차감 | UI 비활성화로 선제 방지 |
| `StockOverflowException` | 관리자 재고 보충 | 오류 메시지, 보충 취소 |
| `AdminAuthException` | 관리자 인증 | 재입력 대기 |
| `InvalidRecipeException` | 데이터 로드 | 로드 실패 알림, 이전 데이터 유지 |

> 상세 결제/재고 흐름은 원본 system_flow.md (pmg-ic-pbl/docs/) 참고
