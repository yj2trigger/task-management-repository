# 수어 → 한국어 번역: 데이터 실측과 키포인트 파이프라인(A) 설계 기록

> 작성: 2026-09-20 (세션 중 실측 기준)
> 짝 문서: [수어-번역-의사결정-2026-09-20.md](./수어-번역-의사결정-2026-09-20.md) — 같은 날 다른 세션이 쓴 의사결정 기록. **일부 수치가 이 문서의 실측과 어긋난다(§4 정정).**
> 상태: 설계 브레인스토밍 진행 중. 코드는 아직 없고, 조사용 일회성 스크립트만 썼다(§7).

---

## 1. 현재 위치 한 장 요약

| 항목 | 결정 · 상태 |
|---|---|
| 최종 결과물 | 연속 수어 → 한국어 문장 번역 (사용자 선택) |
| 분해 | 하위 프로젝트 4개: **A** 키포인트 추출·데이터 준비 / **B** 단어 인식기 / **C** 연속 수어 → gloss 열 / **D** gloss 열 → 한국어 |
| 시작점 | **A** (B·C가 의존하는 기반) |
| 파이프라인 구조 | 영상 → gloss → 한국어 2단(cascaded) 권장. gloss·한국어 라벨이 양쪽에 다 있어 단계별 검증이 가능 |
| A 접근안 | **636은 제공 XML(OpenPose 2D)을 변환, 103은 MediaPipe로 추출** (사용자 승인) |
| 다음 | 설계 섹션 1·2(통일 포맷, 어댑터) 승인 대기 → 섹션 3(분할·수행자) → 스펙 문서 → 구현 계획 |

승인 대기 중인 열린 질문 2개: **엉덩이 2점 포함 여부**(§5), **636 수행자 ID 확보 방법**(§6).

---

## 2. 결정 로그 (시간순)

| # | 결정 | 근거 |
|---|---|---|
| 1 | 작업을 Architectural로 분류, 스펙→계획 순으로 진행 | 새 프로젝트라 이어받을 코드가 없음 |
| 2 | 목표 = 연속 수어 → 한국어 문장 번역 | 사용자 선택(다른 후보: 단어 인식만 / gloss 열까지) |
| 3 | 4개 하위 프로젝트로 분해, A부터 | A가 B·C의 공통 기반. D는 A~C와 독립이며 `pairs.tsv`로 지금도 가능 |
| 4 | 636 처리 방향은 "벤치마크 후 결정" | 전수 MediaPipe 재추출이 현실적인지 숫자로 확인이 필요했음 |
| 5 | 벤치마크로 636 전수 재추출 불가 확인 (§3.3) | 약 28일 |
| 6 | XML vs MediaPipe 비교로 변환 타당성 실측 (§3.4) | 손 오차 중앙값 약 0.1 |
| 7 | A 접근안 = 636 XML 변환 + 103 MediaPipe | 위 두 실측 |

---

## 3. 실측 결과

### 3.1 103 (수어 영상) — 라벨 확인

`[라벨]01_real_word_morpheme.zip`(0.11GB)의 정면(F) 48,000개 전수를 집계했다.

| 항목 | 실측 |
|---|---|
| 규모 | WORD 3000개 × 수행자 REAL01~16(집계상 17종) × 5시점(D/F/L/R/U). 시점당 47,994개 |
| 라벨 형태 | JSON 1개 = 클립 1개. `data[0]`에 `start`, `end`, `attributes[0].name`(gloss). 예: WORD0001 = "고민", 1.743~3.103초 / 영상 4.867초 |
| 수행자 ID | 파일명 `REALxx`, 폴더 `morpheme/01~16`이 대응. 수행자로 보이나 문서로 확인한 것은 아님 |
| 고유 gloss | 2,963개 (3,000 ID 중 232개 gloss가 여러 ID에 걸침) |
| 라벨 불일치 | 264개 ID는 수행자마다 gloss 표기가 다름 → 클래스 확정 전 정리 필요 |
| 결측 | F 시점 11개는 `data`가 비어 있음 |
| 단어 구간 | 중앙값 1.57초(47프레임), p95 2.7초(81프레임). 영상 중앙값 4.13초 안에서 시작 오프셋 중앙값 1.5초 |

같은 폴더에 받아 둔 다른 라벨 zip: `real_sen_morpheme`(SEN 2000문장 × 16 × 5 = 160,000개), `crowd_morpheme`(17,000개), `syn_sen_keypoint`(0.81GB). **문장·crowd·syn 데이터는 열어 보지 않았다.**

### 3.2 636 (재난 안전 정보 수어영상) — 문장-gloss 쌍

`수어스크립트_TL.zip`을 xlsx 131개로 파싱해 `pairs.tsv`(200,986블록)를 만들었다. **확장자는 `.zip`이지만 형식은 7-Zip이다.**

| 항목 | 실측 |
|---|---|
| 블록 / 고유 한국어 문장 | 200,986 / 148,844 (공식 페이지의 164,375문장과 차이. 원인 미확인) |
| 대면 / 비대면 | 127,608 / 73,378 |
| 카테고리 | 자연재난 66,266 · 사회재난 60,376 · 날씨예보 59,519 · 기타재난 14,825 |
| gloss | 26,023종, 3,138,538토큰. 끝 숫자·`#`를 떼면 19,145종 |
| gloss/문장 | 중앙값 15, 평균 15.6, p95 28, 최대 100 |
| 롱테일 | 5토큰 이하 17,173종, 1토큰 9,598종 |
| 상위 커버리지 | 상위 100종 52.2%, 500종 81.0%, 1,000종 88.8%, 3,000종 95.9% |
| 한국어 문장 길이 | 중앙값 54자, p95 84자, 최대 221자 |

**파싱 누락:** gloss/시작/종료 값 개수가 안 맞으면 그 행을 통째로 버리는 코드였다. 로그상 MISMATCH가 102개 파일, 합계 39,295건(WEATHER-1_1 한 파일이 17,365건). 위 gloss 통계는 이 누락을 반영하지 못했고, 누락 토큰 비율은 **아직 세지 않았다.**

### 3.3 MediaPipe 처리속도 벤치마크

환경: i5-1340P 16스레드, RAM 15.7GB, NVIDIA GPU 없음. 1080p 30fps 영상 각 150프레임(103·636 결과가 사실상 같음).

| 설정 | 1프로세스 fps | 4프로세스 합계 | 8프로세스 합계 |
|---|---|---|---|
| Holistic complexity 1 | 19.5 | 38~43 | 41~48 |
| Holistic complexity 0 | 24 | 37~40 | 47~50 |
| Pose+Hands 분리 (c0 / c1) | 23 / 14 | 미측정 | 미측정 |

- 병렬 효과가 작다: 8프로세스에서도 합계가 약 45fps에서 멈춤. 640폭 축소는 fps 이득이 없다(병목은 추론).
- 150프레임 짧은 측정이라 장시간 열 제한은 반영되지 않았다. 실제는 더 느릴 수 있다.

전수 재추출 예상 (합계 45fps 가정, 37~50fps면 ±20%):

| 대상 | 프레임 | 소요 |
|---|---|---|
| 103 정면(F) 전체 | 약 590만 | 약 36시간 |
| 103 정면, 단어 구간 ±0.5초만 | 약 절반 | 약 18시간 (추정, 미측정) |
| 636 전체 | 약 1억 1,800만 (샘플 27개 평균 588프레임 × 201,026) | 약 28~30일 |
| 636 전체, 15fps | 약 5,900만 | 약 14일 |

### 3.4 636 XML(OpenPose 2D) vs MediaPipe Holistic

636 샘플 27영상, 17,957프레임. 손 오차는 손목~중지 MCP 길이 대비 21점 평균, 상체는 어깨너비 대비.

| 항목 | 결과 |
|---|---|
| 손 오차 | 왼손 중앙값 0.081 / p90 0.126, 오른손 0.097 / p90 0.201 |
| 좌우 대응 | 같은 쪽끼리 0.08~0.10, 뒤바꾸면 3.6~3.9 → 좌우 규약 동일 |
| 손 점 순서 | 같은 쪽 점끼리 오차가 작음 → 21점 인덱스 동일 |
| 프레임 정렬 | 오프셋 0에서 최소(0.095), ±1이면 0.22 → 프레임 번호가 그대로 대응 |
| 상체 7점 | 어깨 0.03, 팔꿈치 0.04~0.05, 손목 0.05~0.06 (중앙값) |
| 검출 커버리지 | XML 유효 97%, MediaPipe 검출 99%, 둘 다 96~97% |
| 프레임 간 떨림 | XML 0.203, MediaPipe 0.191 (중앙값) → XML은 사후 평활·보간 데이터가 아님 |

XML 구조: CVAT 내보내기. 프레임마다 `track` 1개(`outside=0`), 라벨은 `pose_keypoints_2d`(25점, BODY_25) · `face_keypoints_2d`(70) · `hand_left/right_keypoints_2d`(21). 점은 `x,y,conf`.

---

## 4. 정정 — 이 기록이 대체하는 기존 주장

### 4.1 짝 문서(수어-번역-의사결정)와의 불일치

| 짝 문서의 주장 | 이 문서의 실측 | 판정 |
|---|---|---|
| 103 단어 라벨 450개 gloss, 160,000클립 | 3,000 WORD ID, 고유 gloss 2,963개, 정면 47,994개(전 시점 약 24만) | 짝 문서 오류. 160,000은 문장(SEN) 쪽 수치와 같음 |
| 636은 제공 키포인트(경로 B) 사용 불가, 경로 A만 가능 | 전수 MediaPipe 재추출은 약 28일로 비현실적. XML 2D는 MediaPipe와 손 오차 중앙값 약 0.1로 변환 가능 | 결론 뒤집힘 |
| json 90GB "절반 도착" | `2.형태소_비수지(json)_TL.zip.irx141`이 **9.03GB에서 정체**(마지막 수정 12:38, 5초 증가 0MB) | 짝 문서 오류 |
| 636 xml은 따로 받아야 해서 용량 추가 | 맞음. 다만 변환안에서는 xml만 받고 mp4는 안 받아도 됨 | 채택안에 유리 |

짝 문서의 "스코프 A/B/C(재난·날씨 vs 길찾기·교통 vs 병행)" 결정은 **아직 열려 있다.** 이 문서의 하위 프로젝트 D는 636 문장 쌍에 기대므로 도메인이 재난·날씨로 한정된다는 점을 인지하고 있어야 한다.

### 4.2 이 세션 안에서의 자체 정정

| 정정 | 내용 |
|---|---|
| 인덱스 비호환 | 지난 대화에서 "OpenPose와 MediaPipe 인덱스가 비호환"이라 했으나, 손 21점과 상체 대응 점은 실측으로 호환 확인. 비호환은 얼굴 쪽 점에만 해당할 가능성이 큼(얼굴은 미비교) |
| gloss당 클립 수 | 103 집계 스크립트의 "gloss당 F 클립수(1~8)"는 계산 오류. 실제로는 gloss당 WORD ID 수였음. 무효. 클립 수는 ID당 약 16개 |
| 636 프레임 추정 | 처음 약 1억 900만(541프레임 기준)으로 말했으나 샘플 27개 평균 588프레임이라 약 1억 1,800만으로 수정. 결론은 그대로 |
| XML 헤더 `size` | 27개 중 4개가 mp4 프레임 수와 다름(117 vs 541 등). 프레임 수는 헤더가 아니라 실제 `track` 수로 세야 함 |

---

## 5. A 설계 초안 (승인 대기)

**섹션 1. 통일 포맷**
- 클립(103 단어) 또는 문장(636)마다 `[T, 55, 2]` 좌표와 `[T, 55]` 검출 마스크. 좌표는 영상 크기로 나눈 0~1 값. 어깨 중심 정규화 등은 B·C 로더가 함(A는 원본 정보를 잃지 않게 둠).
- 55점 = 상체 13(코, 눈 2, 귀 2, 어깨 2, 팔꿈치 2, 손목 2, 엉덩이 2) + 왼손 21 + 오른손 21. 13점 모두 BODY_25·MediaPipe pose 양쪽에 대응이 있음.
- **신뢰도 대신 마스크.** OpenPose 손 점에는 신뢰도가 있지만 MediaPipe 손 점에는 없음. 공통분모인 "검출 여부(0/1)"만 남기며, XML 신뢰도 0.2 미만은 미검출 처리(0.2는 이번 비교에서 쓴 값이며 튜닝한 값이 아님).
- 메타 인덱스 표: 출처, gloss 또는 한국어 문장, 수행자, 프레임 구간, 분할 라벨.

**섹션 2. 어댑터**
- `636 XML → 55점`: `outside=0` 프레임만 읽어 재배열. 좌우는 그대로(측정으로 확인). 프레임 수는 `track` 수로 셈.
- `103 mp4 → 55점`: Holistic complexity 1, 정면(F)만, 라벨 단어 구간 ±0.5초만 추출.
- 검증: 27샘플 비교를 회귀 검사로 남김. 손 오차가 이 수준을 벗어나면 실패.

**열린 질문**
1. **엉덩이 2점:** 웹캠 상반신 화면에서 안 보일 가능성이 큼. 빼면 53점. 지난 대화에서 13점으로 정한 이유가 있다면 유지.
2. **636 수행자 ID(§6)**

---

## 6. 열린 위험 · 미검증

| 항목 | 상태 |
|---|---|
| 636 수행자 독립 분할 | 수행자 ID를 확인한 곳은 형태소 JSON의 `signer.id`(샘플 27개는 전부 signer 87 한 명). 그 JSON 다운로드가 정체. xlsx·XML·파일명에 수행자 정보가 있는지는 열어 보지 않았으므로 먼저 확인하는 것이 대안 |
| 정체된 다운로드 | Innorix 프로세스는 살아 있으나 진행 없음. 사용자가 창 확인 필요 |
| XML 변환의 일반화 | 비교는 수행자 1명(signer 87), 카테고리 1개(COLDWAVE), 27영상. 다른 조건에서도 같다는 보장 없음 |
| 좌표 일치 ≠ 인식 정확도 | 좌표 불일치를 잰 것이지, XML로 학습해 MediaPipe 입력에서 정확도가 유지되는지는 B·C 단계에서만 확인 가능. 완화책: 636 일부를 MediaPipe로도 추출해 검증용 보관 |
| 미비교 | 얼굴 점, 깊이(z) |
| 103 단어/문장 속도 차 | 103 단어 중앙값 47프레임 vs 636 단어(문장 속) 중앙값 약 16프레임. 두 데이터셋의 속도 척도가 3배 가까이 다름 |
| 103 문장·crowd·syn 데이터 | 열어 보지 않음. C에 쓸 수 있는지 미확인 |
| 학습 연산 | NVIDIA GPU 없음(Intel Arc A350M 4GB). B·C 학습은 별도 위험 |

---

## 7. 재현 · 함정 · 위치

**데이터 위치**
- 103: `D:\_대학교\연구과제\데이터셋\수어영상\`
- 636: `D:\_대학교\연구과제\데이터셋\재난안전정보전달을위한수어영상데이터\114.재난 안전 정보 전달을 위한 수어영상 데이터\01.데이터\`
- 샘플: `D:\dummy\` (103 zip, 636 샘플 27개 mp4·xml·json·xlsx)

**조사용 일회성 산출물 (세션 임시 폴더, 사라질 수 있음)**
- `pairs.tsv`(73MB), 파싱 스크립트: 이전 세션 스크래치패드. 재생성은 약 2분.
- 벤치마크·비교 스크립트, mediapipe venv: 이번 세션 스크래치패드. 스크립트는 레포에 보관하지 않았다.

**함정 (재현할 때 시간을 잃기 쉬운 것)**
| 함정 | 대처 |
|---|---|
| `수어스크립트_TL.zip`이 실제로는 7-Zip | `py7zr` 필요. 90GB 형태소 JSON도 같은 형식일 가능성이 큼(다 받은 뒤 7z로만 읽기) |
| 파이썬 3.14에서 구형 Holistic API 사용 불가 가능성 | 3.12 venv에 `mediapipe==0.10.21` 고정(`mp.solutions.holistic` 확인됨) |
| mediapipe complexity 0이 처음 실행 때 모델을 네트워크에서 내려받음 | venv 안 `pose_landmark_lite.tflite` |
| `*.mp4` 파일 검색에 `1.mp4`라는 이름의 폴더가 걸림 | `-File` 필터 사용 |
| XML 헤더 `size` 신뢰 불가 | `track` 수로 프레임 수 계산 |
| 한글 경로 | OpenCV `VideoCapture`가 한글 경로를 열었음(슬래시로 바꿔서). 별도 조치 불필요 |
